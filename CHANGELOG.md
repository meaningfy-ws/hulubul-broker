# Changelog

All notable changes to this project are documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Changed
- Repository modernized to the Meaningfy project-setup standard: dedicated
  root tool configs, a single `Makefile` task-runner surface (tox removed),
  the OpenSpec spine pinned to the `meaningfy` schema, a canonical
  `CLAUDE.md`, and the `hulubul` package promoted out of `src/`.
