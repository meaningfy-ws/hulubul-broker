[mypy]
python_version = 3.10
strict = True
explicit_package_bases = True

# LinkML-generated, never hand-edited (ADR-019) — `make pydantic` overwrites
# this wholesale, so type errors here are the generator's concern, not ours.
[mypy-hulubul.core.models.domain.hulubul_models]
ignore_errors = True
