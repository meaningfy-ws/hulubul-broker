sonar.projectKey=meaningfy-ws_hulubul-broker
sonar.organization=meaningfy-ws
sonar.sources=src
sonar.tests=tests
sonar.python.version=3.10
sonar.python.coverage.reportPaths=reports/coverage.xml

# Not yet wired into CI: no SONAR_TOKEN/project registration exists for this
# specific repo yet, though the org runs a licensed SonarQube instance
# (see f98729f). Add a CI step once that registration happens.
